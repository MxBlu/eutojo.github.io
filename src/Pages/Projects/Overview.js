import React from 'react';
import { Layout } from 'antd';
import '../../index.css';
// const { Content } = Layout;

export default function Profile(props) {
  return (
    // <Content style={{ padding: '0 50px', marginTop: 64 }}>
      // <div style={{ background: '#fff', padding: 24, marginTop: 30 }}>
        <div>
        <h1>
          Welcome!
        </h1>
        <p>
          Unfortunately, this section is still under construction, but I hope you stick around to view the final product!
        </p>
        <p>
          <b>NOTE:</b> The URL does not work properly :(
        </p>
       </div>

      // <div style={{ background: '#fff', padding: 24, marginTop: 30 }}>
        // Hello
      // </div>
    // </Content>
  );
}